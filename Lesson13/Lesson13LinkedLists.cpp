#include <iostream>
using namespace std;

int main() {
	/*
	linked lists are much faster which means they have a time complexity 
	that is much lower than the O(N) of normal lists O(1) for adding an element 
	although arrays are better at acessing an element
	for array O(1) while O(N) for linked lists
	*/
}
